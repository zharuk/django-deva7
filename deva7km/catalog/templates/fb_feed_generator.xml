<?xml version="1.0" encoding="UTF-8"?>
{% load i18n %}
<price>
    <date>{{ current_date|date:"Y-m-d H:i" }}</date>
    <firmName>{% trans "Дева" %}</firmName>
    <firmId>{{ firm_id|default:"" }}</firmId>
    <rate>{{ exchange_rate|default:"1" }}</rate>

    <categories>
        {% for category in categories %}
        <category>
            <categoryId>{{ category.id }}</categoryId>
            <categoryName>{{ category.name }}</categoryName>
            <parentId>{{ category.parent_id|default:"" }}</parentId>
        </category>
        {% endfor %}
    </categories>

    <items>
        {% for product in products %}
        <item id="{{ product.sku }}" selling_type="u">
            <itemId>{{ product.sku }}</itemId>
            <name>{{ product.title }}</name>
            <description>{{ product.description }}</description>
            <categoryId>{{ product.category_id }}</categoryId>

            <priceuah>
                {% if product.sale_price|default:0 > 0 %}
                    {{ product.sale_price }}
                {% else %}
                    {{ product.price }}
                {% endif %}
            </priceuah>

            {% if product.get_total_stock|default:0 > 0 %}
            <inStock>{{ product.get_total_stock }}</inStock>
            <available>true</available>
            {% else %}
            <inStock>0</inStock>
            <available>false</available>
            {% endif %}

            <guarantee>
                <guaranteeType>{% blocktrans %}гарантия производителя{% endblocktrans %}</guaranteeType>
                <guaranteeLength>{{ product.guarantee_period|default:"1" }}</guaranteeLength>
            </guarantee>

            {% if product.collage_image_url and product.collage_image_url != "None" %}
            <image>{{ product.collage_image_url }}</image>
            {% endif %}
            {% for image in product.get_images_by_colors %}
            <image>{{ image }}</image>
            {% endfor %}

            <vendor>{% trans "Дева" %}</vendor>
            <vendorCode>{{ product.sku }}</vendorCode>

            {% if product.get_colors %}
            <param name="{% trans 'Цвет' %}">{{ product.get_colors }}</param>
            {% endif %}
            {% if product.get_sizes %}
            <param name="{% trans 'Размер' %}">{{ product.get_sizes }}</param>
            {% endif %}

            <param name="{% trans 'Состояние' %}">{% trans "новое" %}</param>
            <param name="{% trans 'Пол' %}">{% trans "женский" %}</param>
            <param name="{% trans 'Возрастная группа' %}">{% trans "взрослый" %}</param>

            <keywords>{{ product.keywords|default:"" }}</keywords>

            <status>
                {% if product.is_active %}
                    {% trans "active" %}
                {% else %}
                    {% trans "not active" %}
                {% endif %}
            </status>

            <minimum_order_quantity>1</minimum_order_quantity>
            <quantum>1</quantum>
        </item>
        {% endfor %}
    </items>
</price>
